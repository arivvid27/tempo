name: tempo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  epubx: ^3.0.0
  html: ^0.15.4
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
  assets:
    - .env

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

